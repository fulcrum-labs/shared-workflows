name: CI Gate (Reusable)

on:
  workflow_call:
    inputs:
      node-version:
        type: string
        default: '24'
      check-command:
        type: string
        default: 'pnpm run ci:local'

permissions:
  contents: read

jobs:
  quality:
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    concurrency:
      group: ci-quality-${{ github.repository }}-${{ github.ref }}
      cancel-in-progress: true
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Resolve pnpm version
        id: pnpm-version
        run: |
          python3 - <<'PY' >> "$GITHUB_OUTPUT"
          import json
          from pathlib import Path

          version = "10.28.0"
          package_json = Path("package.json")
          if package_json.exists():
            data = json.loads(package_json.read_text())
            package_manager = data.get("packageManager")
            if isinstance(package_manager, str) and package_manager.startswith("pnpm@"):
              version = package_manager.split("@", 1)[1]
            else:
              volta = data.get("volta")
              if isinstance(volta, dict):
                volta_pnpm = volta.get("pnpm")
                if isinstance(volta_pnpm, str) and volta_pnpm:
                  version = volta_pnpm

          print(f"version={version}")
          PY

      - uses: pnpm/action-setup@v4
        with:
          version: ${{ steps.pnpm-version.outputs.version }}

      - uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node-version }}
          cache: 'pnpm'

      - run: pnpm install --frozen-lockfile

      - name: CI gate (typecheck + lint + test + build)
        run: ${{ inputs.check-command }}

      - name: Lockfile integrity
        run: |
          if [ ! -f pnpm-lock.yaml ]; then
            echo "::error::pnpm-lock.yaml missing"
            exit 1
          fi
          if [ -f package-lock.json ]; then
            echo "::error::package-lock.json found — use pnpm only"
            exit 1
          fi
          if [ -f yarn.lock ]; then
            echo "::error::yarn.lock found — use pnpm only"
            exit 1
          fi

      - name: pnpm audit
        run: pnpm audit --audit-level=high

      - name: Accessibility (axe-core)
        run: |
          if grep -q '"test:a11y"' package.json 2>/dev/null; then
            pnpm run test:a11y
          else
            echo "::notice::No test:a11y script — skipping"
          fi

      - name: Performance (Lighthouse)
        continue-on-error: true
        run: |
          if grep -q '"test:perf"' package.json 2>/dev/null; then
            pnpm run test:perf
          else
            echo "::notice::No test:perf script — skipping"
          fi

      - name: Dead code (Knip)
        continue-on-error: true
        run: |
          if grep -q '"knip"' package.json 2>/dev/null; then
            pnpm exec knip
          else
            echo "::notice::Knip not configured — skipping"
          fi

  gitleaks:
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Install gitleaks
        run: |
          curl -sSL -o /tmp/gitleaks.tar.gz \
            https://github.com/gitleaks/gitleaks/releases/download/v8.21.2/gitleaks_8.21.2_linux_x64.tar.gz
          tar -xzf /tmp/gitleaks.tar.gz -C /tmp
          sudo mv /tmp/gitleaks /usr/local/bin/gitleaks

      - name: Gitleaks scan
        run: gitleaks detect --source . --no-banner --redact

  semgrep:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    container:
      image: returntocorp/semgrep:latest
    steps:
      - uses: actions/checkout@v4

      - name: Semgrep scan
        run: |
          semgrep scan \
            --config=p/security-audit \
            --config=p/owasp-top-ten \
            --config=p/typescript \
            --error \
            --severity=ERROR \
            --quiet

  grype:
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4

      - name: Grype vulnerability scan
        uses: anchore/scan-action@v6
        with:
          path: '.'
          fail-build: true
          severity-cutoff: high
